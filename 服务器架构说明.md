# 腾讯云服务器架构文档

## 📋 概述

本文档描述了部署在腾讯云服务器 (175.178.158.54) 上的Web服务架构，主要用于家庭/办公室监控和照片管理系统。

---

## 🏗️ 系统架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                        腾讯云服务器                          │
│                      175.178.158.54                          │
└────────────────────┬────────────────────────────────────────┬─┘
                     │                                    │
        ┌────────────┴────────────┐              ┌──────────┴──────────┐
        │      端口 80 (HTTP)     │              │     端口 22 (SSH)   │
        └────────────┬────────────┘              └─────────────────────┘
                     │
        ┌────────────┴────────────┐
        │       Nginx (反向代理)   │
        └────────────┬────────────┘
                     │
    ┌────────────────┼────────────────┐
    │                │                │
┌───┴─────┐    ┌───┴─────┐   ┌───┴─────┐
│ 7788端口 │    │ 7789端口 │   │ 根路径   │
│(书房温度)│    │(办公室温度)│   │ (照片站) │
└───┬─────┘    └───┬─────┘   └───┬─────┘
    │               │               │
外部代理       内部Node.js      PHP-FPM
               端口3789        端口9000
                     │
            ┌────────┴────────┐
            │    MQTT 1883     │
            │   (空调远程控制)  │
            └─────────────────┘
```

---

## 📦 服务清单

### 1. Nginx Web服务器
**端口**: 80, 7788, 7789

**功能**:
- 反向代理
- 静态文件服务
- SSL支持
- 负载均衡

**配置文件位置**:
- 主配置: `/etc/nginx/nginx.conf`
- 站点配置: `/etc/nginx/sites-available/`
- 启用站点: `/etc/nginx/sites-enabled/`

### 2. PHP-FPM (PHP FastCGI Process Manager)
**端口**: 9000 (Unix Socket: `/var/run/php/php8.3-fpm.sock`)

**功能**:
- 处理PHP请求
- 照片上传API
- 数据库操作

**配置文件**: `/etc/php/8.3/fpm/`

### 3. MySQL 数据库
**端口**: 3306

**数据库**: `photo_gallery`

**功能**:
- 存储照片元数据
- 管理相册目录
- 用户认证信息

**配置文件**: `/etc/mysql/my.cnf`

### 4. Node.js 办公室温度监控服务
**端口**: 3789 (内部)

**功能**:
- 接收ESP32上传的温湿度数据
- 提供Web监控页面
- 数据实时显示

**服务信息**:
- 工作目录: `/opt/esp32-receiver/`
- 主程序: `server.js`
- Systemd服务: `esp32-office.service`

### 5. MQTT Broker (Mosquitto)
**端口**: 1883

**功能**:
- 提供MQTT消息代理服务
- 支持ESP32远程控制空调
- 允许匿名访问

**配置文件**: `/etc/mosquitto/mosquitto.conf`, `/etc/mosquitto/conf.d/custom.conf`

**服务信息**:
- 服务名: `mosquitto`
- 监听地址: `0.0.0.0:1883` (允许外部连接)
- 允许匿名: `true`

---

## 🌐 服务端点详解

### 📍 端口 80 - 主网站 (照片管理系统)

#### 路由配置

| 路径 | 方法 | 功能 | 处理方式 |
|------|------|------|---------|
| `/` | GET | 照片展示主页 | index.html |
| `/login` | GET | 登录页面 | login.html |
| `/api/albums` | GET | 获取相册列表 | api.php |
| `/api/albums` | POST | 创建相册 | api.php |
| `/api/albums/{id}` | PUT | 编辑相册 | api.php |
| `/api/albums/{id}` | DELETE | 删除相册 | api.php |
| `/api/photos` | GET | 获取照片列表 | api.php |
| `/api/upload` | POST | 上传照片 | api.php |
| `/api/photos/{id}` | DELETE | 删除照片 | api.php |
| `/uploads/*` | GET | 照片文件 | 静态文件 |

#### 文件结构
```
/var/www/html/
├── index.html          # 主页面
├── login.html          # 登录页面
├── api.php            # API处理脚本
├── uploads/           # 照片存储目录
└── .htaccess          # Apache兼容配置
```

#### 数据库表结构

**albums 表**
```sql
CREATE TABLE albums (
  id INT PRIMARY KEY AUTO_INCREMENT,
  name VARCHAR(255) NOT NULL
);
```

**photos 表**
```sql
CREATE TABLE photos (
  id INT PRIMARY KEY AUTO_INCREMENT,
  title VARCHAR(255),
  date DATE,
  image_url VARCHAR(255),
  album_id INT,
  created_at DATETIME,
  FOREIGN KEY (album_id) REFERENCES albums(id)
);
```

#### 认证方式
- localStorage token: `auth_token`
- 简单认证机制

---

### 📍 端口 7788 - 书房温度监控 (外部代理)

#### 反向代理配置
```nginx
server {
    listen 7788;
    server_name _;
    
    add_header 'Access-Control-Allow-Origin' '*';
    add_header 'Access-Control-Allow-Methods' 'GET, POST, OPTIONS';
    add_header 'Access-Control-Allow-Headers' 'Content-Type';
    
    location / {
        proxy_pass http://sumaj.synology.me:7788;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_connect_timeout 10s;
        proxy_read_timeout 10s;
        proxy_send_timeout 10s;
    }
}
```

**说明**: 此端口代理到外部的Synology NAS设备

---

### 📍 端口 7789 - 办公室温度监控

#### 架构
```
外部访问 (7789)
    ↓
Nginx 反向代理
    ↓
Node.js 服务 (3789)
```

#### Nginx配置
```nginx
server {
    listen 7789;
    server_name _;

    add_header 'Access-Control-Allow-Origin' '*';

    location / {
        proxy_pass http://127.0.0.1:3789;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}
```

#### Node.js服务 (`/opt/esp32-receiver/server.js`)

**接收数据接口**:
- URL: `http://175.178.158.54:7789/update`
- 方法: POST
- Content-Type: application/json
- 数据格式:
  ```json
  {
    "temperature": 26.5,
    "humidity": 65.0
  }
  ```

**监控页面**:
- URL: `http://175.178.158.54:7789/`
- 方法: GET
- 功能: 显示实时温湿度数据
- 自动刷新: 10秒

---

### 📍 端口 1883 - MQTT Broker (空调远程控制)

#### MQTT服务信息
- Broker地址: `175.178.158.54`
- 端口: `1883`
- 协议: MQTT 3.1.1 / 3.1
- 认证: 允许匿名访问
- QoS支持: 0, 1, 2

#### MQTT配置文件 (`/etc/mosquitto/conf.d/custom.conf`)
```ini
# 监听所有网络接口
listener 1883 0.0.0.0

# 允许匿名连接
allow_anonymous true

# 日志设置
log_dest file /var/log/mosquitto/mosquitto.log
log_type all
```

#### 防火墙配置
```bash
# 允许MQTT端口
sudo ufw allow 1883/tcp

# 查看防火墙状态
sudo ufw status
```

#### 服务管理命令
```bash
# 启动服务
sudo systemctl start mosquitto

# 停止服务
sudo systemctl stop mosquitto

# 重启服务
sudo systemctl restart mosquitto

# 查看服务状态
sudo systemctl status mosquitto

# 查看实时日志
sudo journalctl -u mosquitto -f

# 查看MQTT日志文件
sudo tail -f /var/log/mosquitto/mosquitto.log
```

#### MQTT主题与消息格式

**控制主题**: `office/ac/control`

**消息格式 (JSON)**:
```json
{
  "action": "on"
}
```

**支持的操作**:
- `{"action": "on"}` - 开启空调
- `{"action": "off"}` - 关闭空调

#### 测试MQTT连接

**使用 mosquitto_pub/mosquitto_sub (需安装 mosquitto-clients)**:
```bash
# 订阅主题，接收消息
mosquitto_sub -h 175.178.158.54 -p 1883 -t "office/ac/control"

# 发送开机指令
mosquitto_pub -h 175.178.158.54 -p 1883 -t "office/ac/control" -m '{"action":"on"}'

# 发送关机指令
mosquitto_pub -h 175.178.158.54 -p 1883 -t "office/ac/control" -m '{"action":"off"}'
```

**使用MQTT客户端工具**:
- MQTTX (跨平台桌面/移动端)
- MQTT Explorer (Windows/Mac/Linux)
- HiveMQ Web Client (在线工具)

#### Systemd服务配置
```ini
[Unit]
Description=ESP32 Office Monitor
After=network.target

[Service]
Type=simple
WorkingDirectory=/opt/esp32-receiver
ExecStart=/usr/bin/node /opt/esp32-receiver/server.js
Restart=always
User=ubuntu

[Install]
WantedBy=multi-user.target
```

#### 服务管理命令
```bash
# 启动服务
sudo systemctl start esp32-office

# 停止服务
sudo systemctl stop esp32-office

# 重启服务
sudo systemctl restart esp32-office

# 查看服务状态
sudo systemctl status esp32-office

# 查看服务日志
sudo journalctl -u esp32-office -f

# 开机自启
sudo systemctl enable esp32-office
```

---

## 🔄 数据流程

### 温湿度数据上传流程

```
ESP32 设备
  ↓ (WiFi)
  ↓ POST {"temperature": 26.5, "humidity": 65.0}
  ↓
Nginx (端口7789)
  ↓ 反向代理
  ↓
Node.js (端口3789)
  ↓ 解析JSON
  ↓ 存储到内存变量
  ↓
Web页面 (GET /)
  ↓ 返回HTML
  ↓ 显示数据
  ↓ 10秒自动刷新
```

### MQTT空调控制流程

```
MQTT客户端 (手机/网页/脚本)
  ↓ TCP连接
  ↓ 连接到 175.178.158.54:1883
  ↓ 订阅/发布消息
  ↓
Mosquitto Broker (1883)
  ↓ 路由消息到订阅者
  ↓
ESP32 设备 (FreeRTOS MQTT任务)
  ↓ 接收JSON消息
  ↓ 解析 {"action":"on"/"off"}
  ↓ 发送红外指令到空调
  ↓ 空调执行开关
```

### 照片上传流程

```
用户浏览器
  ↓ 登录验证
  ↓ 选择照片
  ↓
Nginx (端口80)
  ↓ 转发到PHP
  ↓
PHP-FPM (api.php)
  ↓ 验证文件
  ↓ 保存到 /var/www/html/uploads/
  ↓ 提取EXIF信息
  ↓ 存储到MySQL
  ↓
返回成功响应
```

---

## 🔧 系统服务管理

### Nginx
```bash
# 启动/停止/重启
sudo systemctl start nginx
sudo systemctl stop nginx
sudo systemctl restart nginx

# 测试配置
sudo nginx -t

# 重载配置
sudo nginx -s reload
```

### PHP-FPM
```bash
sudo systemctl start php8.3-fpm
sudo systemctl stop php8.3-fpm
sudo systemctl restart php8.3-fpm
```

### MySQL
```bash
sudo systemctl start mysql
sudo systemctl stop mysql
sudo systemctl restart mysql
```

---

## 🔐 安全配置

### 防火墙 (UFW)
```bash
# 查看状态
sudo ufw status

# 允许端口
sudo ufw allow 22/tcp   # SSH
sudo ufw allow 80/tcp   # HTTP
sudo ufw allow 443/tcp  # HTTPS
sudo ufw allow 7788/tcp # 书房温度
sudo ufw allow 7789/tcp # 办公室温度
sudo ufw allow 1883/tcp # MQTT (空调控制)
```

### 2. 数据库安全
- 数据库用户: `photo_user`
- 数据库密码: `PhotoSecurePass123!`
- 仅允许本地连接: `localhost`

### 3. 文件权限
```bash
/var/www/html/ - 755 (ubuntu:ubuntu)
/var/www/html/uploads/ - 755 (www-data:www-data)
/opt/esp32-receiver/ - 755 (root:root)
```

---

## 📊 监控与日志

### 日志文件位置

| 服务 | 日志路径 | 说明 |
|------|---------|------|
| Nginx | `/var/log/nginx/access.log` | 访问日志 |
| Nginx | `/var/log/nginx/error.log` | 错误日志 |
| PHP-FPM | `/var/log/php8.3-fpm.log` | PHP日志 |
| MySQL | `/var/log/mysql/error.log` | MySQL日志 |
| Node.js | `journalctl -u esp32-office` | 温度监控日志 |
| Mosquitto | `/var/log/mosquitto/mosquitto.log` | MQTT日志 |
| Mosquitto | `journalctl -u mosquitto` | MQTT系统日志 |

### 查看实时日志
```bash
# Nginx访问日志
sudo tail -f /var/log/nginx/access.log

# Nginx错误日志
sudo tail -f /var/log/nginx/error.log

# Node.js服务日志
sudo journalctl -u esp32-office -f

# Mosquitto MQTT日志
sudo journalctl -u mosquitto -f
```

---

## 🚀 部署ESP32设备

### ESP32代码配置

**WiFi配置**:
```cpp
const char* ssid = "您的WiFi名称";
const char* password = "您的WiFi密码";
```

**服务器配置**:
```cpp
const char* serverUrl = "http://175.178.158.54:7789/update";
const unsigned long uploadInterval = 5000;  // 5秒上传一次
```

**MQTT配置**:
```cpp
const char* mqttServer = "175.178.158.54";
const int mqttPort = 1883;
const char* mqttTopic = "office/ac/control";
```

**上传数据格式**:
```json
{
  "temperature": 26.5,
  "humidity": 65.0
}
```

**MQTT消息格式**:
```json
{
  "action": "on"   // 或 "off"
}
```

### ESP32硬件连接

| 传感器 | 引脚 | 说明 |
|--------|------|------|
| DHT22 VCC | 3.3V | 电源 |
| DHT22 GND | GND | 地线 |
| DHT22 DATA | GPIO14 | 数据 |
| ST7789 TFT | SPI接口 | 显示屏 |
| 红外模块 RX | GPIO16 | 串口接收 |
| 红外模块 TX | GPIO17 | 串口发送 |

---

## 📱 前端访问

### 照片管理系统
- URL: `http://175.178.158.54/`
- 功能: 照片浏览、上传、相册管理

### 书房温度监控
- URL: `http://175.178.158.54:7788/`
- 功能: 实时温湿度显示

### 办公室温度监控
- URL: `http://175.178.158.54:7789/`
- 功能: 实时温湿度显示

### MQTT空调控制
- Broker地址: `175.178.158.54:1883`
- 控制主题: `office/ac/control`
- 功能: 远程开关空调
- 测试工具: MQTTX, MQTT Explorer

---

## 🔧 故障排查

### 1. 温度监控页面显示"离线"
**原因**: ESP32未上传数据超过30秒
**解决**:
- 检查ESP32是否正常运行
- 检查WiFi连接
- 检查Node.js服务状态: `sudo systemctl status esp32-office`
- 查看服务日志: `sudo journalctl -u esp32-office -n 50`

### 2. 照片上传失败
**原因**: PHP-FPM服务异常或磁盘空间不足
**解决**:
- 检查PHP-FPM: `sudo systemctl status php8.3-fpm`
- 检查磁盘空间: `df -h`
- 检查uploads目录权限: `ls -la /var/www/html/uploads/`

### 3. Nginx 502错误
**原因**: 后端服务未启动
**解决**:
- 检查PHP-FPM: `sudo systemctl status php8.3-fpm`
- 检查Node.js: `sudo systemctl status esp32-office`
- 检查Socket文件: `ls -la /var/run/php/`

### 4. 端口无法访问
**原因**: 防火墙或Nginx配置问题
**解决**:
```bash
# 检查防火墙
sudo ufw status

# 检查端口监听
sudo netstat -tlnp | grep -E "80|7788|7789|1883"

# 测试Nginx配置
sudo nginx -t

# 重启Nginx
sudo systemctl restart nginx
```

### 5. MQTT连接失败
**原因**: 防火墙未开放或Mosquitto配置问题
**解决**:
```bash
# 检查防火墙是否开放1883端口
sudo ufw status | grep 1883

# 开放MQTT端口
sudo ufw allow 1883/tcp

# 检查Mosquitto配置
sudo cat /etc/mosquitto/conf.d/custom.conf

# 确认监听地址为 0.0.0.0:1883

# 重启Mosquitto服务
sudo systemctl restart mosquitto

# 查看连接日志
sudo journalctl -u mosquitto -f
```

### 6. MQTT消息无法接收
**原因**: ESP32未订阅主题或网络问题
**解决**:
```bash
# 检查ESP32 MQTT连接状态
# 通过ESP32串口监视器查看输出

# 检查ESP32是否订阅了正确主题
# 应该看到: "订阅主题: office/ac/control"

# 测试MQTT发布消息
mosquitto_pub -h 175.178.158.54 -p 1883 -t "office/ac/control" -m '{"action":"on"}'

# 查看Mosquitto日志确认消息是否到达
sudo tail -f /var/log/mosquitto/mosquitto.log
```

---

## 📝 备份策略

### 1. 数据库备份
```bash
# 备份数据库
mysqldump -u photo_user -p photo_gallery > backup_$(date +%Y%m%d).sql

# 恢复数据库
mysql -u photo_user -p photo_gallery < backup_20240101.sql
```

### 2. 照片备份
```bash
# 备份uploads目录
tar -czf photos_backup_$(date +%Y%m%d).tar.gz /var/www/html/uploads/
```

### 3. 配置文件备份
```bash
# 备份Nginx配置
tar -czf nginx_config_backup.tar.gz /etc/nginx/

# 备份Node.js服务
tar -czf esp32_receiver_backup.tar.gz /opt/esp32-receiver/

# 备份Mosquitto配置
tar -czf mosquitto_config_backup.tar.gz /etc/mosquitto/
```

### 4. 自动备份脚本 (可选)
创建 `/opt/backup/backup.sh`:
```bash
#!/bin/bash
BACKUP_DIR="/opt/backup"
DATE=$(date +%Y%m%d)

# 创建备份目录
mkdir -p $BACKUP_DIR

# 备份数据库
mysqldump -u photo_user -p'PhotoSecurePass123!' photo_gallery > $BACKUP_DIR/photo_gallery_$DATE.sql

# 备份照片
tar -czf $BACKUP_DIR/photos_$DATE.tar.gz /var/www/html/uploads/

# 备份配置文件
tar -czf $BACKUP_DIR/config_$DATE.tar.gz /etc/nginx/ /etc/mosquitto/

# 删除30天前的备份
find $BACKUP_DIR -name "*.sql" -mtime +30 -delete
find $BACKUP_DIR -name "*.tar.gz" -mtime +30 -delete

echo "Backup completed: $DATE"
```

设置定时任务 (每天凌晨2点备份):
```bash
crontab -e
# 添加以下行
0 2 * * * /opt/backup/backup.sh >> /var/log/backup.log 2>&1
```

---

## 📞 联系信息

- 服务器IP: 175.178.158.54
- SSH端口: 22
- 管理用户: ubuntu

---

## 📄 版本历史

| 日期 | 版本 | 说明 |
|------|------|------|
| 2025-02-05 | v1.2 | 添加MQTT服务支持空调远程控制；更新ESP32功能说明 |
| 2024-01-29 | v1.0 | 初始架构文档 |

---

## 📌 配置文件清单

### Nginx配置文件
- `/etc/nginx/nginx.conf` - 主配置文件
- `/etc/nginx/sites-available/default` - 默认站点配置
- `/etc/nginx/sites-enabled/` - 启用的站点符号链接

### Mosquitto配置文件
- `/etc/mosquitto/mosquitto.conf` - 主配置文件
- `/etc/mosquitto/conf.d/custom.conf` - 自定义配置 (监听1883, 允许匿名)

### PHP-FPM配置文件
- `/etc/php/8.3/fpm/php.ini` - PHP配置
- `/etc/php/8.3/fpm/pool.d/www.conf` - 进程池配置

### MySQL配置文件
- `/etc/mysql/my.cnf` - 主配置文件
- `/etc/mysql/mysql.conf.d/mysqld.cnf` - MySQL服务配置

### Systemd服务文件
- `/etc/systemd/system/esp32-office.service` - 办公室温度监控服务
- `/lib/systemd/system/mosquitto.service` - MQTT Broker服务 (默认)

---

## 📖 相关文档

- `使用说明.md` - ESP32项目使用说明
- `main.cpp` - ESP32主程序源代码
- `platformio.ini` - PlatformIO项目配置

---

**文档生成时间**: 2025-02-05
